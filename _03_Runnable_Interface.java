public class _03_Runnable_Interface {
    /*
     * - Runnable interface를 이용한 구현
     * 
     * Runnable Interface를 구현해 Thread로 동작하는 클래스를 만든다.
     * Runnable Interface 구현을 통해 여러 작업을 동시에 실행해 본다.
     * 
     * Thread를 이용하는 class는 앞서 소개한 Thread class 확장 외에 Runnable interface를 통해서도 정의할 수 있다.
     * 앞서 본 Thread class의 hireachy를 보면 Thread class도 Runnable interface의 구현으로 정의되었음을 알 수 있다.
     * 
     * Runable interface는 Thread class를 이용해 실행할 수 있도록 필요한 기능을 method로 선언하고 있을 뿐
     * 실제 실행을 위해서는 Quiz-01에서 보는 바와 같이 별도의 Thread class를 이용한 object 생성이 필요하다.
     */
    public static void main(String[] args) {
        
    }
}
